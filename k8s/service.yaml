apiVersion: v1
kind: Service
metadata:
  name: linguamaster-api
  labels:
    app.kubernetes.io/name: linguamaster
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: linguamaster
    app.kubernetes.io/component: api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
